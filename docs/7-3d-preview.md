# 3D preview

The 3D preview should update live whenever parameters change.

Recommended stack:

```text
Three.js
React Three Fiber
@react-three/drei
```

The preview should support:

```text
orbit
zoom
pan
reset camera
wireframe toggle
transparent bin toggle
```

A transparent Gridfinity bin around the insert would be especially useful for demonstrating fit.
